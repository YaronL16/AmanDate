1. Get frontend URL:
{{- range .Values.frontend.ingress.hosts }}
  http{{ if and $.Values.frontend.ingress.tls $.Values.frontend.ingress.tls.clusterIssuerName }}s{{ end }}://{{ .host }}
{{- end }}

{{- if and $.Values.frontend.ingress.tls $.Values.frontend.ingress.tls.clusterIssuerName }}

  TLS enabled via cert-manager ClusterIssuer: {{ $.Values.frontend.ingress.tls.clusterIssuerName }}
  Generated TLS Secret: {{ include "amandate.frontend.fullname" . }}-tls
{{- end }}

2. Backend service DNS:
  {{ include "amandate.backend.fullname" . }}.{{ .Release.Namespace }}.svc.cluster.local:8000

3. CloudNativePG read-write endpoint:
  {{ include "amandate.cnpgRwServiceName" . }}.{{ .Release.Namespace }}.svc.cluster.local:{{ .Values.database.port }}

4. Vault prerequisites:
  Ensure Vault Secrets Operator is installed and a VaultAuth named {{ .Values.vaultStaticSecret.vaultAuthRef }} exists in namespace {{ .Release.Namespace }}.
