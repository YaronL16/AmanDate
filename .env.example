DB_NAME=amandate
DB_USER=amandate
DB_PASSWORD=amandate
POSTGRES_PORT=5432

API_PORT=8000

APP_ENV=development
CORS_ALLOWED_ORIGINS=http://localhost:5173

# `DB_HOST` should be `db` for Docker Compose API/tests and `localhost` for host runs.
DB_HOST=localhost
DB_PORT=5432

# Optional override for environments that provide a full URL:
# DATABASE_URL=postgresql+psycopg2://amandate:amandate@localhost:5432/amandate


FRONTEND_PORT=5173
VITE_API_BASE_URL=http://localhost:8000

# Admin view configuration
ENABLE_ADMIN_VIEW=true
ADMIN_USER_ID=yaron
VITE_ENABLE_ADMIN_VIEW=true
VITE_ADMIN_USER_ID=yaron